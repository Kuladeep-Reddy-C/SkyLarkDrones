# Base URL of the backend API.
# Leave blank to use same-origin / the Vite dev proxy (recommended for local dev).
# For split deploys (Vercel frontend + Render backend), set this to the Render URL:
#   VITE_API_BASE=https://skylark-bi-agent-api.onrender.com
VITE_API_BASE=
